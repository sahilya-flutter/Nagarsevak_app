import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nagarsevak.app',
  appName: 'नगरसेवक - Nagarsevak',
  webDir: 'dist',
};

export default config;
